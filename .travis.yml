sudo: false
language: node_js
node_js:
  - "9"

addons:
  chrome: stable

notifications:
  email: false

git:
  depth: 3

before_install:
  - npm install -g npm@~5.3.0

install:
  - npm install

script:
   - npm run lint
   - ng run test
   - npm run build:library

